# Agent Notes

- When looking for tools to run, prefer the ones that are installed via Homebrew first
- Do not rebuild the frontend project just to verify routine frontend edits; the dev server is expected to be running.
- When asked to reset database, do not try to apply migration
