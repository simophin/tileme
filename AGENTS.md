# Agent Notes

- Prefer the Homebrew-provided Node/npm toolchain first: `env PATH=/home/linuxbrew/.linuxbrew/bin:$PATH npm ...`.
- Do not rebuild the frontend project just to verify routine frontend edits; the dev server is expected to be running.
